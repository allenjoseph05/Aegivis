apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentblackbox.fullname" . }}-proxy
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agentblackbox.labels" . | nindent 4 }}
    app.kubernetes.io/component: proxy
spec:
  replicas: {{ .Values.proxy.replicaCount }}
  selector:
    matchLabels:
      {{- include "agentblackbox.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: proxy
  template:
    metadata:
      labels:
        {{- include "agentblackbox.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: proxy
    spec:
      containers:
        - name: proxy
          image: "{{ .Values.proxy.image.repository }}:{{ .Values.proxy.image.tag }}"
          imagePullPolicy: {{ .Values.proxy.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: ABB_BACKEND_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "agentblackbox.fullname" . }}-config
                  key: ABB_BACKEND_URL
            - name: ABB_ORG_ID
              valueFrom:
                configMapKeyRef:
                  name: {{ include "agentblackbox.fullname" . }}-config
                  key: ABB_ORG_ID
            - name: ABB_LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "agentblackbox.fullname" . }}-config
                  key: ABB_LOG_LEVEL
            - name: ABB_PII_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: {{ include "agentblackbox.fullname" . }}-config
                  key: ABB_PII_ENABLED
            - name: ABB_METRICS_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: {{ include "agentblackbox.fullname" . }}-config
                  key: ABB_METRICS_ENABLED
            - name: ABB_OTEL_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: {{ include "agentblackbox.fullname" . }}-config
                  key: ABB_OTEL_ENABLED
            - name: ABB_BACKEND_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.proxy.secretName }}
                  key: ABB_BACKEND_API_KEY
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.proxy.secretName }}
                  key: OPENAI_API_KEY
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.proxy.secretName }}
                  key: ANTHROPIC_API_KEY
                  optional: true
          resources:
            {{- toYaml .Values.proxy.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
